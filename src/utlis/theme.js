import { theme as cTheme } from '@chakra-ui/core';

const theme = {
  ...cTheme,
  fonts: {
    body: 'Jaldi, system-ui, sans-serif',
    heading: 'Zilla Slab, system-ui, sans-serif',
  }
};

export default theme;